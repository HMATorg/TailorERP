# Origin of apps/admin (the tenant admin SPA) — used to build the impersonation
# handoff link on the Tenant Detail page (D-060). Defaults to the local dev port
# if unset.
VITE_ADMIN_APP_URL=http://localhost:5173
